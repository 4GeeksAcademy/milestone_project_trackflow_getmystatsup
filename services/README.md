# Supplier API

FastAPI + TinyDB supplier directory service for TrackFlow.

## Run

```bash
uv sync
uv run uvicorn api.main:app --reload
```

## Seed

```bash
uv run seed
```

## Test

```bash
uv run pytest
```
