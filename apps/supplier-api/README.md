# Supplier API

FastAPI + TinyDB supplier directory service for TrackFlow.

## Run

```bash
uv sync
uv run uvicorn app.main:app --reload
```

## Seed

```bash
uv run seed
```

## Test

```bash
uv run pytest
```
